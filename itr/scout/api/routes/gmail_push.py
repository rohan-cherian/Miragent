# Emptied deliberately — the Gmail push receiver moved to
# scout/gmail/ingest_api.py (same endpoints: /gmail/push, /gmail/sync,
# /gmail/status, /health). This routes/ package belongs to the console API
# (Slice-1 Task 24, Sutej).
